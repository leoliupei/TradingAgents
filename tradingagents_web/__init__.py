"""Local-first web workspace for TradingAgents.

This package intentionally sits beside the upstream ``tradingagents`` engine
instead of inside it, so upstream syncs can stay low-conflict.
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
